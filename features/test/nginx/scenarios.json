{
    "install": {
        "build": {
            "dockerfile": "Dockerfile",
            "options": [
                "--add-host=host.docker.internal:host-gateway"
            ]
        },
        "features": {
            "./nginx": {
                "version": "1.27.2",
                "stableOnly": "false"
            }
        }
    }
}