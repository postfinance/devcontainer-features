{
    "install-exact": {
        "build": {
            "dockerfile": "Dockerfile",
            "options": [
                "--add-host=host.docker.internal:host-gateway"
            ]
        },
        "features": {
            "./go": {
                "version": "1.23.1!"
            }
        }
    },
    "install-non-exact": {
        "build": {
            "dockerfile": "Dockerfile",
            "options": [
                "--add-host=host.docker.internal:host-gateway"
            ]
        },
        "features": {
            "./go": {
                "version": "1.23"
            }
        }
    }
}