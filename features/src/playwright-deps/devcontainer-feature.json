{
    "id": "playwright-deps",
    "version": "0.1.0",
    "name": "Playwright Dependencies",
    "description": "A package which installs the needed dependencies to run Playwright.",
    "options": {
        "installChromiumDeps": {
            "type": "boolean",
            "default": true,
            "description": "A flag to indicate if the dependencies for Chromium should be installed."
        },
        "installFirefoxDeps": {
            "type": "boolean",
            "default": true,
            "description": "A flag to indicate if the dependencies for Firefox should be installed."
        },
        "installWebkitDeps": {
            "type": "boolean",
            "default": true,
            "description": "A flag to indicate if the dependencies for Webkit should be installed."
        }
    },
    "containerEnv": {
        "PLAYWRIGHT_HTML_HOST": "0.0.0.0"
    }
}