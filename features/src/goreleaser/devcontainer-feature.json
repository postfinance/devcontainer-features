{
    "id": "goreleaser",
    "version": "0.1.0",
    "name": "GoReleaser",
    "description": "A package which installs GoReleaser.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "2.4.8",
                "2.3"
            ],
            "default": "latest",
            "description": "The version of GoReleaser to install."
        }
    },
    "containerEnv": {
        "PATH": "/usr/local/goreleaser:${PATH}"
    }
}