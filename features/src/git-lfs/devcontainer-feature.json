{
    "id": "git-lfs",
    "version": "0.2.0",
    "name": "Git LFS",
    "description": "Installs Git LFS.",
    "options": {
        "version": {
            "type": "string",
            "proposals": [
                "latest",
                "3.7.0",
                "3.6"
            ],
            "default": "latest",
            "description": "The version of Git LFS to install."
        },
        "downloadUrl": {
            "type": "string",
            "default": "",
            "proposals": [
                "https://mycompany.com/artifactory/github-generic-remote/git-lfs/git-lfs/releases/download",
                "https://mycompany.com/artifactory/git-lfs-download-generic-remote"
            ],
            "description": "The download URL to use for Git LFS binaries."
        }
    }
}